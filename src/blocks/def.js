// Blockly-like JSON block definition.
// args0 declares inputs/fields → compiler auto-extracts → generator gets {values, fields, statements, next, target}
export function def(config) { return config; }
